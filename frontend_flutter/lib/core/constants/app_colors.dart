import 'package:flutter/material.dart';

/// App-wide color palette following Navy & Electric Green theme
class AppColors {
  AppColors._();

  // Primary Colors
  static const Color navy = Color(0xFF0A192F);
  static const Color darkNavy = Color(0xFF020C1B);
  static const Color electricGreen = Color(0xFF00FF41);
  
  // Accent Colors
  static const Color lightNavy = Color(0xFF172A45);
  static const Color slate = Color(0xFF8892B0);
  static const Color lightSlate = Color(0xFFCCD6F6);
  
  // Status Colors
  static const Color success = Color(0xFF00FF41);
  static const Color error = Color(0xFFFF5370);
  static const Color warning = Color(0xFFFFA500);
  static const Color info = Color(0xFF64FFDA);
  
  // Text Colors
  static const Color textPrimary = Color(0xFFE6F1FF);
  static const Color textSecondary = Color(0xFF8892B0);
  static const Color textDisabled = Color(0xFF495670);
}
